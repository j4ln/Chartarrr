{
  "name": "chartarrr",
  "description": "Assisting in exploratory testing on the web via session based test charters.",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "npx prettier --write . && npx eslint . && npx web-ext lint -s ./src",
    "start": "npx web-ext run -s ./src --devtools",
    "prepare": "husky"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.7",
    "@types/eslint__js": "^8.42.3",
    "@types/eslint-config-prettier": "^6.11.3",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^9.0.11",
    "prettier": "^3.3.2",
    "typescript-eslint": "^7.15.0",
    "web-ext": "^8.2.0"
  },
  "peerDependencies": {
    "typescript": "^5.5.3"
  }
}
